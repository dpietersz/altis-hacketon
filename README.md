# altis-hacketon

Monorepo for the Altis hackathon project.

```
altis-hacketon/
├── databricks/        # Notebooks, jobs, libs for Azure Databricks
│   └── notebooks/
│       └── 01_connect_storage.py
├── frontend/          # Web UI (stack TBD)
└── README.md
```

## Getting started

- **Databricks notebook:** see `databricks/README.md` for how to import
  `01_connect_storage.py` into the workspace. It is the first thing to run
  end-to-end — it proves we can list both containers and read an `.xlsx`.
- **Frontend:** placeholder — pick a stack before scaffolding.
